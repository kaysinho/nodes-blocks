import { LeftZerosPipe } from './left-zeros.pipe';

describe('LeftZerosPipe', () => {
  it('create an instance', () => {
    const pipe = new LeftZerosPipe();
    expect(pipe).toBeTruthy();
  });
});
